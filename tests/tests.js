import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
import sinon from 'sinon';
import UserComponent from '../src/userComponent_bk';
import UsersListComponent from '../src/usersListComponent_bk';


describe('Test suite for `TDD in React using Enzyme` workshop', function() {
  it('Working placeholder assertion', () => {
    expect(true).to.equal(true);
  });
});
